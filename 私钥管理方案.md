二、推荐架构：Safe + Module（不修改 Safe 核心合约）

 每个基金的 Gnosis Safe（标准合约，不修改）
 ├── Owner Key（平台控制）→ Safe Owner
 │   └── 权限：提款、转账、暂停、配置变更、紧急恢复
 │
 ├── PolymarketTradingModule（自定义 Safe Module）
 │   └── Trader Key（Agent 使用）→ Module 的 authorized caller
 │       └── 权限：仅 placeOrder / cancelOrder，受白名单和限额约束
 │
 └── Safe 合约本身不做任何修改，保证 EIP-1271 兼容

 三、密钥生成方案

 方案对比

 ┌───────────────────────────────┬────────┬───────────┬────────────┬──────────────┐
 │             方案              │ 安全性 │   成本    │   复杂度   │   适合阶段   │
 ├───────────────────────────────┼────────┼───────────┼────────────┼──────────────┤
 │ HD 钱包 + AWS KMS（推荐 MVP） │ 高     │ 低        │ 中         │ MVP / 早期   │
 ├───────────────────────────────┼────────┼───────────┼────────────┼──────────────┤
 │ MPC（Fireblocks / Fordefi）   │ 极高   │ 高（$$$） │ 低（托管） │ 规模化后     │
 ├───────────────────────────────┼────────┼───────────┼────────────┼──────────────┤
 │ Turnkey（TEE）                │ 极高   │ 中        │ 低         │ MVP 或规模化 │
 ├───────────────────────────────┼────────┼───────────┼────────────┼──────────────┤
 │ HSM（硬件）                   │ 极高   │ 高        │ 高         │ 机构级       │
 └───────────────────────────────┴────────┴───────────┴────────────┴──────────────┘

 MVP 推荐：HD 钱包 + AWS KMS

 密钥生成流程

 1. 离线生成 Master Seed（BIP-39 助记词，24 词）
    └── 加密存储在离线介质（如加密 USB），作为灾难恢复备份

 2. 使用 BIP-44 派生路径为每个基金生成两对密钥：
    ├── Owner Key:  m/44'/60'/0'/0/{fundIndex}
    └── Trader Key: m/44'/60'/0'/1/{fundIndex}

 3. 将派生出的私钥导入 AWS KMS（每个密钥一个 CMK）
    └── 私钥导入后，本地立即销毁明文
    └── 后续所有签名操作都通过 KMS API 完成

 为什么用 HD 钱包？

 - 一个 seed 管理所有密钥：只需安全备份一个 seed，即可恢复所有基金的所有密钥
 - 确定性派生：知道 seed + fundIndex 就能重新推导出密钥，无需逐一备份
 - CEX 标准做法：Binance、Coinbase 等交易所的用户充值地址就是这样生成的

 密钥存储

 ┌──────────────────────────────────────────────────────┐
 │                    AWS KMS                            │
 │                                                       │
 │  CMK-001: Fund#1 Owner Key  (secp256k1)              │
 │  CMK-002: Fund#1 Trader Key (secp256k1)              │
 │  CMK-003: Fund#2 Owner Key  (secp256k1)              │
 │  CMK-004: Fund#2 Trader Key (secp256k1)              │
 │  ...                                                  │
 │                                                       │
 │  ✓ 私钥永远不离开 KMS 硬件边界                          │
 │  ✓ 所有签名操作在 KMS 内部完成                          │
 │  ✓ IAM 控制谁能调用签名 API                            │
 │  ✓ CloudTrail 记录每一次签名操作                        │
 └──────────────────────────────────────────────────────┘

 ┌──────────────────────────────────────────────────────┐
 │            离线冷存储（灾难恢复用）                      │
 │                                                       │
 │  加密 USB × 2（异地存放）                               │
 │  └── Master Seed（BIP-39 24 词助记词）                  │
 │  └── KMS Key ID 映射表（fundIndex → CMK ID）           │
 └──────────────────────────────────────────────────────┘

 四、签名服务架构

 ┌─────────────┐     ┌──────────────────┐     ┌──────────┐
 │  主应用服务  │ ──→ │  隔离签名服务     │ ──→ │  AWS KMS │
 │             │     │  (独立子网)       │     │          │
 │ 构造未签名   │     │ 验证请求合法性    │     │ 执行签名  │
 │ 交易/消息    │     │ 调用 KMS 签名    │     │ 返回签名  │
 │             │ ←── │ 返回签名结果      │     │          │
 │ 广播已签名   │     │                  │     │          │
 │ 交易到链上   │     │ 记录审计日志      │     │          │
 └─────────────┘     └──────────────────┘     └──────────┘

 关键安全原则：
 ✓ 签名服务部署在独立私有子网，无公网访问
 ✓ 只接受来自主应用的 signed API 请求
 ✓ 私钥永远不进入应用内存
 ✓ 所有签名请求记录不可篡改的审计日志

 五、用户注册 & 基金创建完整流程

 1. 管理人提交创建基金申请
        ↓
 2. 后端通过 HD 派生生成 Owner Key 和 Trader Key
        ↓
 3. 将两个私钥导入 AWS KMS，获得 CMK ID
        ↓
 4. 用 Owner Key 的地址部署 Gnosis Safe（1-of-1 multisig）
        ↓
 5. 部署 PolymarketTradingModule 并 enable 到 Safe 上
        ↓
 6. 将 Trader Key 地址设为 Module 的 authorized trader
        ↓
 7. 在 Polymarket CLOB 注册 Safe 地址（signature_type = 2）
        ↓
 8. 基金创建完成，Agent 可通过 Trader Key 签名交易

 六、安全规范 Checklist

 密钥生成

 - 使用加密安全随机数生成器（CSPRNG）
 - HD 派生遵循 BIP-32/39/44 标准
 - Master Seed 在离线环境生成，生成后立即加密存储
 - 派生的私钥导入 KMS 后立即从内存和磁盘销毁明文

 密钥存储

 - 私钥只存在于 KMS/HSM 内部，永不导出
 - Master Seed 加密存储在离线介质，异地双备份
 - 数据库中只存公钥/地址和 KMS CMK ID，不存私钥

 访问控制

 - KMS 签名 API 只有签名服务的 IAM Role 能调用
 - Owner Key 签名需要额外审批流程（大额操作）
 - Trader Key 签名有速率限制和金额限制
 - 所有管理员访问启用 MFA

 运行时安全

 - 签名服务部署在隔离子网
 - 私钥永不出现在日志、错误信息、环境变量中
 - CloudTrail 记录每次 KMS 调用
 - 异常签名模式告警（异常金额、异常频率、异常目标地址）

 密钥轮换

 - Trader Key：每季度轮换（从 seed 派生新 index，更新 Module 授权）
 - Owner Key：年度轮换或事件触发（更新 Safe owner）
 - 轮换流程文档化并定期演练

 灾难恢复

 - Master Seed 可恢复所有密钥
 - KMS CMK ID 映射表有离线备份
 - 恢复流程文档化并定期演练

 七、与 CEX 充值地址管理的类比

 你的直觉完全正确，场景非常类似：

 ┌──────────┬──────────────────────────┬──────────────────────────────────┐
 │          │       CEX 充值地址       │         你们的基金 Safe          │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 地址生成 │ HD 派生，每用户一个      │ HD 派生，每基金一个 Safe         │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 私钥管理 │ KMS/HSM，永不导出        │ KMS，永不导出                    │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 资金归集 │ 热钱包 → 冷钱包自动归集  │ 交易资金在 Safe 内，提款走 Owner │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 签名服务 │ 隔离微服务 + KMS         │ 隔离签名服务 + KMS               │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 灾难恢复 │ Master Seed 恢复所有地址 │ Master Seed 恢复所有密钥         │
 └──────────┴──────────────────────────┴──────────────────────────────────┘

 唯一区别：CEX 充值地址通常是普通 EOA，你们用的是 Safe 智能合约钱包，多了一层合约级权限控制。

 八、规模化后的升级路径

 MVP 用 HD + AWS KMS 足够。当基金规模增大后，可考虑：

 1. Fireblocks MPC：私钥分片存储，即使后端被攻破也无法单独签名
 2. Turnkey TEE：私钥在可信执行环境中，50-100ms 签名延迟
 3. 多签 Owner：Safe 从 1-of-1 升级为 2-of-3 multisig，增加安全冗余


-------------------
   方式一：密钥派生服务隔离（推荐 MVP）

  ┌──────────────────────────────────────────────────────────┐
  │            密钥派生服务（独立、隔离、高安全）               │
  │                                                           │
  │  1. Master Seed 加密存储在 AWS KMS 的 CMK 中              │
  │     （Seed 本身作为数据被 KMS 加密，不是作为签名密钥）       │
  │  2. 派生服务启动时从 KMS 解密 Seed 到内存                  │
  │  3. 在内存中执行 BIP-44 派生 → 生成 Owner/Trader 私钥     │
  │  4. 将派生出的私钥立即导入 KMS 作为新的签名密钥             │
  │  5. 从内存中清除 Seed 和派生私钥的明文                     │
  │                                                           │
  │  安全措施：                                                │
  │  - 这个服务只在创建基金时被调用（低频）                      │
  │  - 部署在隔离子网，无公网访问                               │
  │  - 只有特定 IAM Role 能触发                                │
  │  - 可以部署在 AWS Nitro Enclave 中（内存也受保护）          │
  └──────────────────────────────────────────────────────────┘

  Seed 在内存中的暴露窗口很短（毫秒级），但确实存在风险。

  方式二：完全不让后端接触 Seed（更安全）

  ┌──────────────────────────────────────────────────────────┐
  │  方案 A：离线派生                                         │
  │                                                           │
  │  1. 运维人员在离线/气隙机器上用 Seed 派生密钥              │
  │  2. 手动将私钥导入 AWS KMS                                │
  │  3. 后端永远不接触 Seed                                   │
  │  缺点：无法自动化，每次创建基金需要人工介入                  │
  │                                                           │
  │  方案 B：不用 HD，直接 KMS 生成                            │
  │                                                           │
  │  1. 调用 AWS KMS CreateKey API 直接生成 secp256k1 密钥    │
  │  2. KMS 自己生成随机密钥，私钥从未离开 KMS                 │
  │  3. 后端只拿到公钥/地址和 CMK ID                          │
  │  缺点：没有 Master Seed 统一恢复能力，                     │
  │        每个密钥独立，需要单独备份 KMS CMK                   │
  │  优点：最安全，私钥从生到死都在 KMS 里，零暴露              │
  └──────────────────────────────────────────────────