Polymarket Safe Wallet 兼容性 & 密钥管理方案

 一、Polymarket 完全支持 Gnosis Safe 钱包

 Polymarket 原生支持三种钱包类型：

 ┌─────────────┬───────────────────┬───────────────────────────────────┐
 │    类型     │ Signature Type ID │               说明                │
 ├─────────────┼───────────────────┼───────────────────────────────────┤
 │ EOA         │         0         │ 标准以太坊钱包                    │
 ├─────────────┼───────────────────┼───────────────────────────────────┤
 │ POLY_PROXY  │         1         │ Magic Link / Email 登录的代理钱包 │
 ├─────────────┼───────────────────┼───────────────────────────────────┤
 │ GNOSIS_SAFE │         2         │ 浏览器钱包用户，推荐类型          │
 └─────────────┴───────────────────┴───────────────────────────────────┘

 - Polymarket 自己的用户账户就是 1-of-1 Gnosis Safe
 - 签名验证走 EIP-1271 标准（isValidSignature()）
 - 官方参考实现：Polymarket/safe-wallet-integration

 二、推荐架构：Safe + Module（不修改 Safe 核心合约）

 每个基金的 Gnosis Safe（标准合约，不修改）
 ├── Owner Key（平台控制）→ Safe Owner
 │   └── 权限：提款、转账、暂停、配置变更、紧急恢复
 │
 ├── PolymarketTradingModule（自定义 Safe Module）
 │   └── Trader Key（Agent 使用）→ Module 的 authorized caller
 │       └── 权限：仅 placeOrder / cancelOrder，受白名单和限额约束
 │
 └── Safe 合约本身不做任何修改，保证 EIP-1271 兼容

 三、密钥生成与管理方案

 确定方案：离线 HD 派生 + AWS KMS 托管

 Owner Key（平台控制，高价值）

 1. 离线（气隙机器）生成 Master Seed（BIP-39，24 词）
 2. 在离线环境用 BIP-44 派生 Owner Key：m/44'/60'/0'/0/{fundIndex}
 3. 私钥明文加密后离线备份（加密 USB × 2，异地存放）
 4. 将私钥导入 AWS KMS（托管模式）用于日常签名
 5. 离线环境清除所有明文

 关键：不用 KMS 生成模式（无法导出），用导入模式（自己有离线备份兜底）

 Trader Key（交付给用户/Agent，不保留）

 1. 后端生成 Trader Key
 2. 将 Trader Key 地址注册为 Module 的 authorized trader
 3. 将 Trader Key 明文一次性交付给用户（Agent）
 4. 后端只保存 Trader Key 的公钥/地址（用于验证和监控）
 5. 明文从后端内存和存储中彻底删除，平台不保留私钥

 用户丢了 Trader Key → Owner 在 Module 上替换 authorized trader 地址 → 用户生成新密钥即可

 为什么 Trader Key 不保留明文？

 - 平台不持有用户交易密钥 → 无法挪用资金、无法伪造交易
 - 降低平台的安全责任和合规风险
 - 即使后端被攻破，攻击者拿不到 Trader Key
 - 信任模型清晰：平台管钱（Owner），用户管交易（Trader）

 密钥存储架构

 ┌──────────────────────────────────────────────────────┐
 │                    AWS KMS（托管模式）                  │
 │                                                       │
 │  CMK-001: Fund#1 Owner Key  (secp256k1, 导入)        │
 │  CMK-002: Fund#2 Owner Key  (secp256k1, 导入)        │
 │  ...                                                  │
 │  注意：只存 Owner Key，Trader Key 不入 KMS             │
 │                                                       │
 │  ✓ 所有 Owner 签名操作在 KMS 内部完成                   │
 │  ✓ IAM 控制谁能调用签名 API                            │
 │  ✓ CloudTrail 记录每一次签名操作                        │
 └──────────────────────────────────────────────────────┘

 ┌──────────────────────────────────────────────────────┐
 │            离线冷存储（灾难恢复用）                      │
 │                                                       │
 │  加密 USB × 2（异地存放）                               │
 │  ├── Owner Key 私钥明文（加密）                         │
 │  ├── Master Seed（如使用 HD 派生，BIP-39 24 词）        │
 │  └── KMS Key ID 映射表（fundIndex → CMK ID）           │
 └──────────────────────────────────────────────────────┘

 ┌──────────────────────────────────────────────────────┐
 │                平台数据库                               │
 │                                                       │
 │  fund_id | owner_address | trader_address | kms_cmk_id│
 │  不存任何私钥明文                                       │
 └──────────────────────────────────────────────────────┘

 AWS KMS 风险与应对

 ┌───────────────────┬────────────────────────────────────────────────────┐
 │       风险        │                      应对措施                      │
 ├───────────────────┼────────────────────────────────────────────────────┤
 │ AWS 账号被封/欠费 │ Owner Key 有离线备份，可直接用私钥恢复 Safe 控制权 │
 ├───────────────────┼────────────────────────────────────────────────────┤
 │ KMS 区域故障      │ 启用 Multi-Region Key 跨区域复制                   │
 ├───────────────────┼────────────────────────────────────────────────────┤
 │ 误删 CMK          │ KMS 删除有 7-30 天等待期；且有离线备份兜底         │
 ├───────────────────┼────────────────────────────────────────────────────┤
 │ IAM 配置错误      │ AWS 根账号可修复；离线备份可兜底                   │
 └───────────────────┴────────────────────────────────────────────────────┘

 四、签名服务架构

 ┌─────────────┐     ┌──────────────────┐     ┌──────────┐
 │  主应用服务  │ ──→ │  隔离签名服务     │ ──→ │  AWS KMS │
 │             │     │  (独立子网)       │     │          │
 │ 构造未签名   │     │ 验证请求合法性    │     │ 执行签名  │
 │ 交易/消息    │     │ 调用 KMS 签名    │     │ 返回签名  │
 │             │ ←── │ 返回签名结果      │     │          │
 │ 广播已签名   │     │                  │     │          │
 │ 交易到链上   │     │ 记录审计日志      │     │          │
 └─────────────┘     └──────────────────┘     └──────────┘

 关键安全原则：
 ✓ 签名服务部署在独立私有子网，无公网访问
 ✓ 只接受来自主应用的 signed API 请求
 ✓ 私钥永远不进入应用内存
 ✓ 所有签名请求记录不可篡改的审计日志

 五、用户注册 & 基金创建完整流程

 1. 管理人提交创建基金申请
        ↓
 2. 运维在离线气隙机器上从 Master Seed 派生 Owner Key
    → 私钥加密后离线备份（加密 USB × 2，异地存放）
    → 私钥导入 AWS KMS（托管模式），获得 CMK ID
    → 离线环境清除明文
        ↓
 3. 后端生成 Trader Key
    → 不入 KMS，不保留明文
        ↓
 4. 用 Owner Key 地址部署 Gnosis Safe（1-of-1 multisig）
        ↓
 5. 部署 PolymarketTradingModule 并 enable 到 Safe 上
        ↓
 6. 将 Trader Key 地址设为 Module 的 authorized trader
        ↓
 7. 在 Polymarket CLOB 注册 Safe 地址（signature_type = 2）
        ↓
 8. 将 Trader Key 明文一次性交付给用户（Agent）
    → 后端清除 Trader Key 明文，只保留地址
        ↓
 9. 基金创建完成
    → 平台用 Owner Key（via KMS）管理提款和配置
    → Agent 用 Trader Key 通过 Module 交易

 六、安全规范 Checklist

 密钥生成

 - 使用加密安全随机数生成器（CSPRNG）
 - HD 派生遵循 BIP-32/39/44 标准
 - Master Seed 在离线环境生成，生成后立即加密存储
 - 派生的私钥导入 KMS 后立即从内存和磁盘销毁明文

 密钥存储

 - 私钥只存在于 KMS/HSM 内部，永不导出
 - Master Seed 加密存储在离线介质，异地双备份
 - 数据库中只存公钥/地址和 KMS CMK ID，不存私钥

 访问控制

 - KMS 签名 API 只有签名服务的 IAM Role 能调用
 - Owner Key 签名需要额外审批流程（大额操作）
 - Trader Key 的交易限制由 Module 合约在链上强制执行
 - 所有管理员访问启用 MFA

 运行时安全

 - 签名服务部署在隔离子网
 - 私钥永不出现在日志、错误信息、环境变量中
 - CloudTrail 记录每次 KMS 调用
 - 异常签名模式告警（异常金额、异常频率、异常目标地址）

 密钥轮换

 - Trader Key：用户丢失时，Owner 在 Module 上替换 authorized trader 地址
 - Owner Key：年度轮换或事件触发（从 seed 派生新 index，更新 Safe owner）
 - 轮换流程文档化并定期演练

 灾难恢复

 - Master Seed 可恢复所有密钥
 - KMS CMK ID 映射表有离线备份
 - 恢复流程文档化并定期演练

 七、与 CEX 充值地址管理的类比

 你的直觉完全正确，场景非常类似：

 ┌──────────┬──────────────────────────┬──────────────────────────────────┐
 │          │       CEX 充值地址       │         你们的基金 Safe          │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 地址生成 │ HD 派生，每用户一个      │ HD 派生，每基金一个 Safe         │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 私钥管理 │ KMS/HSM，永不导出        │ KMS，永不导出                    │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 资金归集 │ 热钱包 → 冷钱包自动归集  │ 交易资金在 Safe 内，提款走 Owner │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 签名服务 │ 隔离微服务 + KMS         │ 隔离签名服务 + KMS               │
 ├──────────┼──────────────────────────┼──────────────────────────────────┤
 │ 灾难恢复 │ Master Seed 恢复所有地址 │ Master Seed 恢复所有密钥         │
 └──────────┴──────────────────────────┴──────────────────────────────────┘

 唯一区别：CEX 充值地址通常是普通 EOA，你们用的是 Safe 智能合约钱包，多了一层合约级权限控制。

 八、规模化后的升级路径

 MVP 用 HD + AWS KMS 足够。当基金规模增大后，可考虑：

 1. Fireblocks MPC：私钥分片存储，即使后端被攻破也无法单独签名
 2. Turnkey TEE：私钥在可信执行环境中，50-100ms 签名延迟
 3. 多签 Owner：Safe 从 1-of-1 升级为 2-of-3 multisig，增加安全冗余